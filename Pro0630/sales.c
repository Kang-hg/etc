#include<stdio.h>

/*
#define YEAR 3
#define PRODUCTS 5

int sum(int scores[YEAR][PRODUCTS]);

int main() {

	int sales[YEAR][PRODUCTS] = { {1, 2, 3}, {4, 5, 6}, {7, 8, 9} };
	int total_sales;

	total_sales = sum(sales);
	printf("총 매출은 %d입니다. \n", total_sales);

}

int sum(int scores[YEAR][PRODUCTS]) {

	int total = 0;
	for (int i = 0; i < YEAR; i++) {
		for (int j = 0; j < PRODUCTS; j++) {
			total += scores[i][j];
		}
	}
	return total;
}

*/