#include<stdio.h>

/*
long fact(int n) {

	printf("factorial(%d)\n", n);

	if (n <= 0) {
		return 1;
	}
	else
		return n * fact(n - 1);
}

int main() {

	int n;
	printf("정수를 입력하세요: ");
	scanf("%d", &n);
	printf("%d!은 %d입니다. \n", n, fact(n));
}

*/