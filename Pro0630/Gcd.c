#include<stdio.h>

/*
int gcd(int x, int y) {

	if (y == 0) {
		return x;
	}
	else
		return gcd(y, x % y);
}

int main() {

	int x, y;
	printf("두 수를 입력하세요: ");
	scanf("%d%d", &x, &y);
	printf("%d, %d의 최대공약수는 %d입니다. \n", x, y, gcd(x, y));
}

*/