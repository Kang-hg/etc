#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*
int main() {
	srand(time(0));
	int user;
	char com[][10] = { "가위", "바위", "보" };
	int comNum;
	
	printf("가위(1), 바위(2), 보(3) 입력 : ");
	scanf("%d", &user);

	comNum = rand() % 3;

	if ((user - 1) - comNum == -2 || (user - 1) - comNum == 1)
	{
		printf("사용자 승\n");
	}
	else if ((user - 1) - comNum == -1 || (user - 1) - comNum == 2)
	{
		printf("컴퓨터 승\n");
	}
	else
	{
		printf("비김\n");
	}
	printf("사용자 : %s, 컴퓨터 : %s\n", com[user-1], com[comNum]);
}
*/