#include<stdio.h>
#include<stdlib.h>

/*
void array_fill(int* A, int size) {
	srand(time(0));

	for (int i = 0; i < size; i++) {
		A[i] = rand();
		printf("%d\n", A[i]);
	}
}

int main() {
	int a[20] = { 0 };
	array_fill(a, sizeof(a)/sizeof(a[0]));
	
}
*/
