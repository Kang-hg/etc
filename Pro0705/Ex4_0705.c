#include<stdio.h>

/*
void array_print(int* A, int size) {

	printf("A[] = { ");
	for (int i = 0; i < size; i++) {
		A[i] = ++ * A;
		printf("%d ", A[i]);
	}
	printf(" }\n\n");
}

int main() {

	int a[20] = { 0 };
	array_print(a, sizeof(a) / sizeof(a[0]));
}
*/
