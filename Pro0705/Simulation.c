#include<stdio.h>
#include<stdlib.h>

/*
void getSensorData(double* p) {
	srand(time(0));

	p[0] = rand() % 100;
	p[1] = rand() % 100;
	p[2] = rand() % 100;
}

int main() {

	double sensorData[3] = { 0 };
	getSensorData(sensorData);

	printf("좌측 센서와 장애물과의 거리: %lf \n", sensorData[0]);
	printf("중간 센서와 장애물과의 거리: %lf \n", sensorData[1]);
	printf("우측 센서와 장애물과의 거리: %lf \n\n", sensorData[2]);
}

*/