#include <stdio.h>

/*
void get_sum_diff(int x, int y, int* p_sum, int* p_diff) {
	*p_sum = x + y;
	*p_diff = x - y;
}

int main() {

	int s, d;
	int x, y;

	printf("정수 두개를 입력하세요: ");
	scanf("%d%d", &x, &y);
	get_sum_diff(x, y, &s, &d);
	printf("두 수의 합: %d\n", s);
	printf("두 수의 차: %d\n", d);
}

*/