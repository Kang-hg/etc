#include<stdio.h>

/*
int main() {

	int i = 10;
	int* pi = NULL;
	pi = &i;
	
	printf("i = %d, pi = %p\n", i, pi);
	(*pi)++;		// pi가 가리키는 대상을 증가
	printf("i = %d, pi = %p\n", i, pi);

	printf("i = %d, pi = %p\n", i, pi);
	*pi++;			// pi 자체를 증가
	printf("i = %d, pi = %p\n", i, pi);
	
}

*/