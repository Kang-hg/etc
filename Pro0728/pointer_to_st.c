#include <stdio.h>
#include <stdlib.h>

/*
struct student {
	int number;
	char name[20];
	double grade;
};

int main() {
	struct student s1 = { 201917141, "강현구", 4.17 };
	struct student* p;

	p = &s1;

	printf("학번: %d 이름: %s 학점: %lf \n", s1.number, s1.name, s1.grade);
	printf("학번: %d 이름: %s 학점: %lf \n", (*p).number, (*p).name, (*p).grade);
	printf("학번: %d 이름: %s 학점: %lf \n", p->number, p->name, p->grade);
}
*/
