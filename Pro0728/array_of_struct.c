#include <stdio.h>
#include <stdlib.h>
#define SIZE 3

/*
struct student {
	int number;
	char name[20];
	double grade;
};

int main() {
	struct student List[SIZE];
	int i;
	
	for (i = 0; i < SIZE; i++)
	{
		printf("학번을 입력하세요: ");
		scanf("%d", &List[i].number);
		printf("이름을 입력하세요: ");
		scanf("%s", List[i].name);
		printf("학점을 입력하세요: ");
		scanf("%lf", &List[i].grade);
	}

	for (i = 0; i < SIZE; i++)
	{
		printf("이름: %s, 학점: %lf, 학번: %d\n", List[i].name, List[i].grade, List[i].number);
	}
}
*/
