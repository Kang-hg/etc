#include <stdio.h>
#include <stdlib.h>

/*
struct student {
	int number;
	char name[10];
	double grade;
};

int main() {
	struct student s1;

	printf("학번을 입력하세요: ");
	scanf("%d", &s1.number);

	printf("이름을 입력하세요: ");
	scanf("%s", s1.name);

	printf("학점을 입력하세요: ");
	scanf("%lf", &s1.grade);

	printf("학번: %d\n", s1.number);
	printf("이름: %s\n", s1.name);
	printf("학점: %lf\n", s1.grade);
}
*/
