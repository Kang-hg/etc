#include <stdio.h>
#include <stdlib.h>

/*
struct vector {
	float x;
	float y;
};

struct vector get_vector_sum(struct vector* a, struct vector* b, struct vector* sum);

int main() {
	struct vector a = { 2.0, 3.0 };
	struct vector b = { 5.0, 6.0 };
	struct vector sum;

	get_vector_sum(&a, &b, &sum);
	printf("벡터의 합은 (%f, %f) 입니다.\n", sum.x, sum.y);
}

struct vector get_vector_sum(struct vector* a, 
							 struct vector* b, struct vector* sum) {
	sum->x = a->x + b->x;
	sum->y = a->y + b->y;
}
*/
